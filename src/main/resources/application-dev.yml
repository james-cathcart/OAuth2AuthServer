spring:
  datasource:
    url: dummy
    username: dummy
    password: dummy
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
token:
  access:
    timeout: 300
  refresh:
    timeout: 600
